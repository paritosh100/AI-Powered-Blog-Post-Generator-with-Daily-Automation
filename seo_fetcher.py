# seo_fetcher.py
def fetch_seo_data(keyword):
    return {
        "search_volume": 200, # higher number = more traffic, lower = niche 
        "keyword_difficulty": 42, # lower the number : easier to rank 
        "avg_cpc": 1.25  # cost per click 
    }
